/**
 * MaroSun C&I Evaluator - Frontend Global Constants
 * Centralized repository for engineering baselines and Moroccan regulatory tariffs.
 */

// Standard Peak Sun Hours (PSH) baseline for Morocco (fallback)
export const DEFAULT_PSH = 1820;

// Standard Performance Ratio (PR) baseline for PV systems
export const DEFAULT_PR = 0.78;

// ANRE Self-consumption tariff (MAD/kWh)
export const TARIFF_SELF_CONSUMPTION = 1.10;

// ANRE Average injection credit (MAD/kWh)
export const TARIFF_INJECTION = 0.195;
